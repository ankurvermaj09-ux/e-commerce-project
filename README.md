# E-Commerce Microservices Backend

## Setup & Index Initialization

To ensure optimal query performance and enforce unique constraints on MongoDB Atlas, run the standalone index creation script:

```bash
python -m backend.scripts.create_indexes
```
